# WalletConnect Project ID (required for WalletConnect)
# Get from: https://cloud.walletconnect.com/
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Uniswap Trading API Key (required for real swaps)
# Get from: https://portal.uniswap.org/
# Without this key, the app will use mock quotes for development
UNISWAP_API_KEY=

# Database (PostgreSQL)
# For local development with SQLite (not recommended for production):
# DATABASE_URL="file:./dev.db"
#
# For Vercel Postgres (automatically set in Vercel):
# DATABASE_URL="postgres://user:password@host:5432/database?sslmode=require"
#
# Vercel will auto-populate this when you create a Postgres database
DATABASE_URL=""

# Cron job secret (for securing cron endpoints)
# Generate a random secret for production
CRON_SECRET=

# Environment
NODE_ENV=development
